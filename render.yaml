services:
  - type: web
    name: global-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: RENDER_PYTHON_VERSION
        value: 3.10.13
